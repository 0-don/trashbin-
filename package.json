{
  "name": "trashbin-plus",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "spicetify-creator",
    "build-local": "spicetify-creator --out=dist --minify",
    "dev": "rm -rf yarn.lock && rm -rf node_modules && yarn && yarn cache clean && yarn --force && concurrently \"spicetify-creator --watch\" \"spicetify watch -e\""
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.8",
    "@types/react": "^18.3.23",
    "@types/react-dom": "^18.3.7",
    "concurrently": "^9.1.2",
    "postcss": "^8.5.4",
    "spicetify-creator": "github:0-don/spicetify-creator",
    "tailwindcss": "^4.1.8"
  }
}
